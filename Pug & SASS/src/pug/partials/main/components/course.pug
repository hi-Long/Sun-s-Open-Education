mixin course(props)
    .course.position-relative.col-12.col-sm-6.col-md-4.col-lg-3.p-0
        include ./course-overview
        +courseOverview(props)
        include ./course-details
        +courseDetail(props)